spring.datasource.url=jdbc:postgresql://localhost:5432/student
spring.datasource.username=
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always

# this file connects the application to the database
# student is the database name
# I changed the pg_hba.conf file in postgresql directory to accept 'no password'
# and also this thing here works because of the new role 'zacwo' in postgresql